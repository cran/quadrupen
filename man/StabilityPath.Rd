% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/StabilityPath-R6Class.R
\name{StabilityPath}
\alias{StabilityPath}
\title{Class StabilityPath}
\description{
Class of object returned by the \code{\link[=QuadrupenFit]{QuadrupenFit$cross_validate()}} method or the
\code{\link[=cross_validate]{cross_validate()}} function. Owns \code{\link[=print]{print()}} and \code{\link[=plot]{plot()}} methods.
}
\examples{

## ------------------------------------------------
## Method `StabilityPath$plot()`
## ------------------------------------------------

\dontrun{
## Simulating multivariate Gaussian with blockwise correlation
## and piecewise constant vector of parameters
beta <- rep(c(0,1,0,-1,0), c(25,10,25,10,25))
Soo  <- matrix(0.75,25,25) ## bloc correlation between zero variables
Sww  <- matrix(0.75,10,10) ## bloc correlation between active variables
Sigma <- Matrix::bdiag(Soo,Sww,Soo,Sww,Soo) + 0.2
diag(Sigma) <- 1
n <- 100
x <- as.matrix(matrix(rnorm(95*n),n,95) \%*\% chol(Sigma))
y <- 10 + x \%*\% beta + rnorm(n,0,10)

## Build a vector of label for true nonzeros
labels <- rep("irrelevant", length(beta))
labels[beta != 0] <- c("relevant")
labels <- factor(labels, ordered=TRUE, levels=c("relevant","irrelevant"))

enet <- elastic_net(x, y, lambda2 = 10, struct = solve(Sigma), minratio = 1e-2)
stab <- stability(enet, n_subsamples = 200)

## Build the plot an recover the selected variable
plot(stab, labels=labels)

cat("\nFalse positives for the randomized Elastic-net with stability selection: ",
     sum(labels[stab$selection()] != "relevant"))
cat("\nDONE.\n")
}
}
\section{Public fields}{
  \if{html}{\out{<div class="r6-fields">}}
  \describe{
    \item{\code{probabilities}}{a \code{Matrix} object containing the
estimated probabilities of selection along the path of solutions.}

    \item{\code{regParam}}{a list with the levels of the regularizing parameters used}

    \item{\code{subsamples}}{a list that contains the folds used for each subsample.}
  }
  \if{html}{\out{</div>}}
}
\section{Active bindings}{
  \if{html}{\out{<div class="r6-active-bindings">}}
  \describe{
    \item{\code{nvar}}{number of variables (without intercept)}

    \item{\code{nobs}}{number of observation/sample size}

    \item{\code{nonzero}}{variables with a non-null probability of selection along the stability path}

    \item{\code{nonzeroprob}}{subset of the probabilities stability path on the nonzero variables}
  }
  \if{html}{\out{</div>}}
}
\section{Methods}{
\subsection{Public methods}{
  \itemize{
    \item \href{#method-StabilityPath-initialize}{\code{StabilityPath$new()}}
    \item \href{#method-StabilityPath-show}{\code{StabilityPath$show()}}
    \item \href{#method-StabilityPath-print}{\code{StabilityPath$print()}}
    \item \href{#method-StabilityPath-selection}{\code{StabilityPath$selection()}}
    \item \href{#method-StabilityPath-plot}{\code{StabilityPath$plot()}}
    \item \href{#method-StabilityPath-clone}{\code{StabilityPath$clone()}}
  }
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-StabilityPath-initialize"></a>}}
\if{latex}{\out{\hypertarget{method-StabilityPath-initialize}{}}}
\subsection{\code{StabilityPath$new()}}{
  Constructor for a \link{StabilityPath} object
Should be called internally by an object \code{\link[=QuadrupenFit]{QuadrupenFit$stability()}}
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{StabilityPath$new(probabilities, regParam, subsamples)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{probabilities}}{a \code{Matrix} object containing the
estimated probabilities of selection along the path of solutions.}
      \item{\code{regParam}}{a list with the levels of the regularizing parameters used}
      \item{\code{subsamples}}{a list that contains the folds used for each subsample.}
    }
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-StabilityPath-show"></a>}}
\if{latex}{\out{\hypertarget{method-StabilityPath-show}{}}}
\subsection{\code{StabilityPath$show()}}{
  User friendly print method
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{StabilityPath$show()}
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-StabilityPath-print"></a>}}
\if{latex}{\out{\hypertarget{method-StabilityPath-print}{}}}
\subsection{\code{StabilityPath$print()}}{
  User friendly print method
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{StabilityPath$print()}
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-StabilityPath-selection"></a>}}
\if{latex}{\out{\hypertarget{method-StabilityPath-selection}{}}}
\subsection{\code{StabilityPath$selection()}}{
  Perform variable selection based on the stability path
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{StabilityPath$selection(
  sel_mode = c("rank", "PFER"),
  cutoff = 0.75,
  PFER = 2,
  nvarsel = floor(self$nobs/log(self$nvar))
)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{sel_mode}}{a character string, either \code{"rank"} or
\code{"PFER"}. In the first case, the selection is based on the
rank of total probabilities by variables along the path: the first
\code{nvarsel} variables are selected (see below). In the second
case, the PFER control is used as described in Meinshausen and
Buhlmannn's paper. Default is \code{"rank"}.}
      \item{\code{cutoff}}{value of the cutoff probability (only relevant when
\code{sel_mode} equals \code{"PFER"}).}
      \item{\code{PFER}}{value of the per-family error rate to control (only
relevant when \code{sel_mode} equals \code{"PFER"}).}
      \item{\code{nvarsel}}{number of variables selected (only relevant when
\code{sel_mode} equals \code{"rank"}. Default is \code{floor(n/log(p))}.}
    }
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-StabilityPath-plot"></a>}}
\if{latex}{\out{\hypertarget{method-StabilityPath-plot}{}}}
\subsection{\code{StabilityPath$plot()}}{
  Produce a plot of the stability path obtained by stability
selection.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{StabilityPath$plot(
  xvar = "lambda",
  title = "Stability path",
  labels = rep("unknown status", self$nvar),
  sel_mode = c("rank", "PFER"),
  cutoff = 0.75,
  PFER = 2,
  nvarsel = min(self$nvar, floor(self$nobs/log(self$nvar)))
)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{xvar}}{variable to plot on the X-axis: either \code{"lambda"}
(first penalty level) or \code{"fraction"} (fraction of the
penalty level applied tune by \eqn{\lambda_1}{lambda1}). Default
is \code{"lambda"}.}
      \item{\code{title}}{title title. If none given, a somewhat appropriate
title is automatically generated.}
      \item{\code{labels}}{an optional vector of labels for each variable in
the path (e.g., 'relevant'/'irrelevant'). See examples.}
      \item{\code{sel_mode}}{a character string, either \code{"rank"} or
\code{"PFER"}. In the first case, the selection is based on the
rank of total probabilities by variables along the path: the first
\code{nvarsel} variables are selected (see below). In the second
case, the PFER control is used as described in Meinshausen and
Buhlmannn's paper. Default is \code{"rank"}.}
      \item{\code{cutoff}}{value of the cutoff probability (only relevant when
\code{sel_mode} equals \code{"PFER"}).}
      \item{\code{PFER}}{value of the per-family error rate to control (only
relevant when \code{sel_mode} equals \code{"PFER"}).}
      \item{\code{nvarsel}}{number of variables selected (only relevant when
\code{sel_mode} equals \code{"rank"}. Default is \code{floor(n/log(p))}.}
      \item{\code{plot}}{logical; indicates if the graph should be
plotted. Default is \code{TRUE}. If \code{FALSE}, only the
\pkg{ggplot2} object is sent back.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    a list with a \pkg{ggplot2} object which can be plotted
via the \code{print} method, and a vector of selected variables
corresponding to method of choice (\code{"rank"} or
\code{"PFER"}).
  }
  \subsection{Examples}{
    \if{html}{\out{<div class="r example copy">}}
    \preformatted{## Simulating multivariate Gaussian with blockwise correlation
## and piecewise constant vector of parameters
beta <- rep(c(0,1,0,-1,0), c(25,10,25,10,25))
Soo  <- matrix(0.75,25,25) ## bloc correlation between zero variables
Sww  <- matrix(0.75,10,10) ## bloc correlation between active variables
Sigma <- Matrix::bdiag(Soo,Sww,Soo,Sww,Soo) + 0.2
diag(Sigma) <- 1
n <- 100
x <- as.matrix(matrix(rnorm(95*n),n,95) \%*\% chol(Sigma))
y <- 10 + x \%*\% beta + rnorm(n,0,10)

## Build a vector of label for true nonzeros
labels <- rep("irrelevant", length(beta))
labels[beta != 0] <- c("relevant")
labels <- factor(labels, ordered=TRUE, levels=c("relevant","irrelevant"))

enet <- elastic_net(x, y, lambda2 = 10, struct = solve(Sigma), minratio = 1e-2)
stab <- stability(enet, n_subsamples = 200)

## Build the plot an recover the selected variable
plot(stab, labels=labels)

cat("\nFalse positives for the randomized Elastic-net with stability selection: ",
     sum(labels[stab$selection()] != "relevant"))
cat("\nDONE.\n")
}
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-StabilityPath-clone"></a>}}
\if{latex}{\out{\hypertarget{method-StabilityPath-clone}{}}}
\subsection{\code{StabilityPath$clone()}}{
  The objects of this class are cloneable with this method.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{StabilityPath$clone(deep = FALSE)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{deep}}{Whether to make a deep clone.}
    }
    \if{html}{\out{</div>}}
  }
}

}
