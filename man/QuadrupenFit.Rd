% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/QuadrupenFit-R6Class.R
\name{QuadrupenFit}
\alias{QuadrupenFit}
\title{Class "QuadrupenFit"}
\description{
Class of object returned by any fitting function of the
\pkg{quadrupen} package (\code{elastic_net} or
\code{bounded_reg}).

This class comes with the usual \code{\link[=predict]{predict()}}, \code{\link[=fitted]{fitted()}}, \code{\link[=coef]{coef()}},
\code{\link[=residuals]{residuals()}}, \code{\link[=show]{show()}}, \code{\link[=print]{print()}} and \code{\link[=deviance]{deviance()}} S3 methods.

Specific R6 methods are available for model extraction \code{\link[=QuadrupenFit]{QuadrupenFit$get_model()}},
cross validation \code{\link[=QuadrupenFit]{QuadrupenFit$cross_validate()}}, stability selection
\code{\link[=QuadrupenFit]{QuadrupenFit$stability_path()}}, criteria derivation \link[=QuadrupenFit]{QuadrupenFit$criteria()}
and plotting \code{\link[=QuadrupenFit]{QuadrupenFit$plot()}}. They come with equivalent S3 methods : \code{\link[=cross_validate]{cross_validate()}},
\code{\link[=stability]{stability()}} and \code{\link[=plot]{plot()}}.

The \code{"path"}plot is available as soon as a fit has been performed.
For the others, the appropriate post-treatments must have been made via the
methods \code{\link[=QuadrupenFit]{QuadrupenFit$criteria()}}, \code{\link[=QuadrupenFit]{QuadrupenFit$cross_validate()}} or
\code{\link[=QuadrupenFit]{QuadrupenFit$stability()}}

All plots functions are given with the default arguments, except for \code{labels} and \code{log_scale}.
If you need more control, please use the dedicated methods: \code{\link[=QuadrupenFit]{QuadrupenFit$plot_path()}},
\code{\link[=InformationCriteria]{InformationCriteria$plot()}}, \code{\link[=CrossValidation]{CrossValidation$plot()}},
\code{\link[=StabilityPath]{StabilityPath$plot()}} or the corresponding S3 methods.

Plot method for regularization path
}
\examples{

## ------------------------------------------------
## Method `QuadrupenFit$plot_path()`
## ------------------------------------------------

\dontrun{
## Simulating multivariate Gaussian with blockwise correlation
## and piecewise constant vector of parameters
beta <- rep(c(0,1,0,-1,0), c(25,10,25,10,25))
cor <- 0.75
Soo <- toeplitz(cor^(0:(25-1))) ## Toeplitz correlation for irrelevant variables
Sww  <- matrix(cor,10,10) ## bloc correlation between active variables
Sigma <- bdiag(Soo,Sww,Soo,Sww,Soo)
diag(Sigma) <- 1
n <- 50
x <- as.matrix(matrix(rnorm(95*n),n,95) \%*\% chol(Sigma))
y <- 10 + x \%*\% beta + rnorm(n,0,10)

## Plot the Lasso path
plot(lasso(x,y), title="Lasso solution path")
## Plot the Elastic-net path
plot(elastic_net(x,y), title = "Elastic-net solution path")
## Plot the Elastic-net path (fraction on X-axis, unstandardized coefficient)
plot(elastic_net(x,y, lambda2=10), standardize=FALSE, xvar="fraction")
## Plot the Bounded regression path (fraction on X-axis)
plot(bounded_reg(x,y, lambda2=10), xvar="fraction")
}

}
\seealso{
See also \code{\link{InformationCriteria}}, \code{\link{CrossValidation}} and
\code{\link{StabilityPath}}

\code{\link[=cross_validate]{cross_validate()}}
Stability selection for Quadrupen object

\code{\link[=stability]{stability()}}
Penalized criteria based on estimation of degrees of freedom

\code{\link[=criteria]{criteria()}}
}
\section{Active bindings}{
  \if{html}{\out{<div class="r6-active-bindings">}}
  \describe{
    \item{\code{nvar}}{number of coefficient (without intercept)}

    \item{\code{nobs}}{sample size}

    \item{\code{dataModel}}{an object with class \code{\link{DataModel}} storing the data}

    \item{\code{major_tuning}}{vector of "leading" tuning parameters (either l1, linf or l2)}

    \item{\code{minor_tuning}}{vector of "minor" tuning parameters (either l1 or l2)}

    \item{\code{is_l2_regularized}}{Boolean indicating if l2 regularization is applied}

    \item{\code{optim_monitoring}}{list monitoring the optimization}

    \item{\code{optim_config}}{list with low level options used for optimization.}

    \item{\code{fitted}}{Matrix of fitted values, each column corresponding to a value of \code{lambda1}.}

    \item{\code{coefficients}}{Matrix (class \code{"dgCMatrix"}) of
coefficients with respect to the original input. The number of
rows corresponds the length of \code{lambda1}.}

    \item{\code{intercept}}{A vector containing the successive values of the
(unpenalized) intercept.
Equals to zero if \code{intercept} has been set to \code{FALSE}.}

    \item{\code{debias}}{logical, should we rely on the debias coefficient of the regularizer (if available) or not}

    \item{\code{residuals}}{Matrix of residuals, each column corresponding to a value of \code{lambda1}.}

    \item{\code{deviance}}{the model deviance}

    \item{\code{degrees_freedom}}{Estimated degree of freedoms for the successive \code{lambda1}.}

    \item{\code{r_squared}}{vector giving the coefficient of determination as a function of lambda1.}

    \item{\code{information_criteria}}{object with class \code{\link{InformationCriteria}} storing various information criteria
(AIC, BIC, GCV, etc) for the current fit.}

    \item{\code{cross_validation}}{object with class \code{\link{CrossValidation}} storing output of CV job.
Only available once method cross_validate has been called.}

    \item{\code{stability_path}}{object with class \code{\link{StabilityPath}} storing output of stability selection.
Only available once method $stability has been called.}
  }
  \if{html}{\out{</div>}}
}
\section{Methods}{
\subsection{Public methods}{
  \itemize{
    \item \href{#method-QuadrupenFit-initialize}{\code{QuadrupenFit$new()}}
    \item \href{#method-QuadrupenFit-show}{\code{QuadrupenFit$show()}}
    \item \href{#method-QuadrupenFit-print}{\code{QuadrupenFit$print()}}
    \item \href{#method-QuadrupenFit-fit}{\code{QuadrupenFit$fit()}}
    \item \href{#method-QuadrupenFit-get_model}{\code{QuadrupenFit$get_model()}}
    \item \href{#method-QuadrupenFit-predict}{\code{QuadrupenFit$predict()}}
    \item \href{#method-QuadrupenFit-cross_validate}{\code{QuadrupenFit$cross_validate()}}
    \item \href{#method-QuadrupenFit-stability}{\code{QuadrupenFit$stability()}}
    \item \href{#method-QuadrupenFit-criteria}{\code{QuadrupenFit$criteria()}}
    \item \href{#method-QuadrupenFit-plot}{\code{QuadrupenFit$plot()}}
    \item \href{#method-QuadrupenFit-plot_path}{\code{QuadrupenFit$plot_path()}}
    \item \href{#method-QuadrupenFit-clone}{\code{QuadrupenFit$clone()}}
  }
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-QuadrupenFit-initialize"></a>}}
\if{latex}{\out{\hypertarget{method-QuadrupenFit-initialize}{}}}
\subsection{\code{QuadrupenFit$new()}}{
  Initialize a \link{QuadrupenFit} model
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{QuadrupenFit$new(data, intercept, regParam)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{data}}{a \link{DataModel} object}
      \item{\code{intercept}}{a logical; should an intercept be included in the mode?}
      \item{\code{regParam}}{a list with two elements, a vector and a scalar, for the regularization}
    }
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-QuadrupenFit-show"></a>}}
\if{latex}{\out{\hypertarget{method-QuadrupenFit-show}{}}}
\subsection{\code{QuadrupenFit$show()}}{
  User friendly print method
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{QuadrupenFit$show()}
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-QuadrupenFit-print"></a>}}
\if{latex}{\out{\hypertarget{method-QuadrupenFit-print}{}}}
\subsection{\code{QuadrupenFit$print()}}{
  User friendly print method
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{QuadrupenFit$print()}
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-QuadrupenFit-fit"></a>}}
\if{latex}{\out{\hypertarget{method-QuadrupenFit-fit}{}}}
\subsection{\code{QuadrupenFit$fit()}}{
  function performing the optimization
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{QuadrupenFit$fit(control)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{control}}{list controlling the optimization process}
    }
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-QuadrupenFit-get_model"></a>}}
\if{latex}{\out{\hypertarget{method-QuadrupenFit-get_model}{}}}
\subsection{\code{QuadrupenFit$get_model()}}{
  Model extraction
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{QuadrupenFit$get_model(selection, type = c("coefficients", "penalty", "index"))}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{selection}}{either a character (model selection criteria) of a scalar (lambda value)}
      \item{\code{type}}{character for the desired output}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    either a vector of coefficients, a scalar or the model index
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-QuadrupenFit-predict"></a>}}
\if{latex}{\out{\hypertarget{method-QuadrupenFit-predict}{}}}
\subsection{\code{QuadrupenFit$predict()}}{
  Predict response for new sample based on the current model
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{QuadrupenFit$predict(newx = NULL, selection = NULL)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{newx}}{matrix of new values for the regressor with which to predict. If omitted, the fitted values are used.}
      \item{\code{selection}}{either a character (model selection criteria) of a scalar (lambda value)}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    a vector of predicted value
Cross-validation for Quadrupen object
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-QuadrupenFit-cross_validate"></a>}}
\if{latex}{\out{\hypertarget{method-QuadrupenFit-cross_validate}{}}}
\subsection{\code{QuadrupenFit$cross_validate()}}{
  Function that computes K-fold cross-validated error of a
\code{quadrupen} fit, possibly on a grid of \code{lambda1}, \code{lambda2}.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{QuadrupenFit$cross_validate(
  K = 10,
  folds = split(sample(1:self$nobs), rep(1:K, length = self$nobs)),
  lambda2 = self$minor_tuning,
  verbose = TRUE,
  cores = 1
)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{K}}{integer indicating the number of folds. Default is 10.}
      \item{\code{folds}}{list of \code{K} vectors that describes the folds to
use for the cross-validation. By default, the folds are randomly
sampled with the specified K. The same folds are used for each
values of \code{lambda2}.}
      \item{\code{lambda2}}{tunes the \eqn{\ell_2}{l2}-penalty (ridge-like) of
the fit. If none is provided, a vector of values is generated and
a CV is performed on a grid of \code{lambda2} and \code{lambda1},
using the same folds for each \code{lambda2}.}
      \item{\code{verbose}}{logical; indicates if the progression (the current
\code{lambda2} should be displayed. Default is \code{TRUE.}}
      \item{\code{cores}}{the number of cores to use. The default uses 1 core
(safer in case your BLAS/LAPACK libraries are multithreaded)}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    an object with class \link{CrossValidation} is sent back and stored as a
field of the original \link{QuadrupenFit} object.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-QuadrupenFit-stability"></a>}}
\if{latex}{\out{\hypertarget{method-QuadrupenFit-stability}{}}}
\subsection{\code{QuadrupenFit$stability()}}{
  Compute the stability path of a (possibly randomized) fitting
procedure as introduced by Meinshausen and Buhlmann (2010).
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{QuadrupenFit$stability(
  n_subsamples = 50,
  subsample_size = floor(self$nobs/2),
  subsamples = replicate(n_subsamples, sample(1:self$nobs, subsample_size), simplify =
    FALSE),
  weakness = 1,
  verbose = TRUE,
  cores = 1
)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{n_subsamples}}{integer indicating the number of subsamplings
used to estimate the selection probabilities. Default is 100.}
      \item{\code{subsample_size}}{integer indicating the size of each subsamples.
Default is \code{floor(n/2)}.}
      \item{\code{subsamples}}{list with \code{subsamples} entries with vectors
describing the folds to use for the stability procedure. By
default, the folds are randomly sampled with the specified
\code{n_subsamples} and \code{subsample_size} argument.}
      \item{\code{weakness}}{Coefficient used for randomizing the weights of each features.
Default is 1` for no randomization. See details below.}
      \item{\code{verbose}}{logical; indicates if the progression should be
displayed. Default is \code{TRUE}.}
      \item{\code{cores}}{the number of cores to use. The default uses 1 core
(safer in case your BLAS/LAPACK libraries are multithreaded)}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    an object with class \link{StabilityPath} is sent back and stored as a
field of the original \link{QuadrupenFit} object.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-QuadrupenFit-criteria"></a>}}
\if{latex}{\out{\hypertarget{method-QuadrupenFit-criteria}{}}}
\subsection{\code{QuadrupenFit$criteria()}}{
  Produce a plot or send back the values of some penalized criteria
accompanied with the vector(s) of parameters selected
accordingly. The default behavior plots the BIC and the AIC (with
respective factor \eqn{\log(n)}{log(n)} and \eqn{2}{2}) yet the user can specify any
penalty.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{QuadrupenFit$criteria(
  penalty = setNames(c(2, log(self$nobs), log(self$nvar), log(self$nobs) + 2 *
    log(self$nvar)), c("AIC", "BIC", "mBIC", "eBIC")),
  sigma = NULL
)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{penalty}}{a vector with as many penalties a desired. The
default contains the penalty corresponding to the AIC and the BIC
(\eqn{2}{2} and \eqn{\log(n)}{log(n)}). Setting the "names"
attribute, as done in the default definition, leads to outputs
which are easier to read.}
      \item{\code{sigma}}{scalar: an estimate of the residual variance. When
available, it is plugged-in the criteria, which may be more
relevant. If \code{NULL} (the default), it is estimated as usual
(see details).}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    an object with class \link{InformationCriteria} is sent back and stored as a
field of the original \link{QuadrupenFit} object.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-QuadrupenFit-plot"></a>}}
\if{latex}{\out{\hypertarget{method-QuadrupenFit-plot}{}}}
\subsection{\code{QuadrupenFit$plot()}}{
  Plot method for QuadrupenFit
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{QuadrupenFit$plot(
  type = c("path", "criteria", "crossval", "stability"),
  log_scale = TRUE,
  labels = NULL
)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{type}}{the type of plot, either \code{"path"} for regularization path;
\code{"criteria"} for BIC-like  information criteria ; \code{"crossval"} for
cross-validation plot ; and \code{"stability"} for stability path.}
      \item{\code{log_scale}}{logical; indicates if a log-scale should be used
when \code{xvar="lambda"}. Default is \code{TRUE}.}
      \item{\code{labels}}{vector indicating the names associated to the plotted
variables. When specified, a legend is drawn in order to identify
each variable. Only relevant when the number of predictor is
small. Remind that the intercept does not count. Default is
\code{NULL}.}
    }
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-QuadrupenFit-plot_path"></a>}}
\if{latex}{\out{\hypertarget{method-QuadrupenFit-plot_path}{}}}
\subsection{\code{QuadrupenFit$plot_path()}}{
  Produce a plot of the solution path of a \link{QuadrupenFit} object.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{QuadrupenFit$plot_path(
  xvar = c("lambda", "fraction", "df"),
  log_scale = TRUE,
  title = paste("Path for", self$penalty),
  standardize = TRUE,
  labels = NULL
)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{xvar}}{variable to plot on the X-axis: either \code{"lambda"}
(\eqn{\ell_1}{l1} penalty level, or
\eqn{\ell_2}{l2} for ridge  and \eqn{\ell_\infty}{l_inf}) or
\code{"fraction"} (\eqn{\ell_1}{l1}-norm
of the coefficients) or \code{df} for estimated degrees of freedom.
Default is set to \code{"lambda"}.}
      \item{\code{log_scale}}{logical; indicates if a log-scale should be used
when \code{xvar="lambda"}. Default is \code{TRUE}.}
      \item{\code{title}}{the title. Default is set to the model name followed
by what is on the Y-axis.}
      \item{\code{standardize}}{logical; standardize the coefficients before
plotting (with the norm of the predictor). Default is \code{TRUE}.}
      \item{\code{labels}}{vector indicating the names associated to the plotted
variables. When specified, a legend is drawn in order to identify
each variable. Only relevant when the number of predictor is
small. Remind that the intercept does not count. Default is
\code{NULL}.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    a \pkg{ggplot2} object .
  }
  \subsection{Examples}{
    \if{html}{\out{<div class="r example copy">}}
    \preformatted{## Simulating multivariate Gaussian with blockwise correlation
## and piecewise constant vector of parameters
beta <- rep(c(0,1,0,-1,0), c(25,10,25,10,25))
cor <- 0.75
Soo <- toeplitz(cor^(0:(25-1))) ## Toeplitz correlation for irrelevant variables
Sww  <- matrix(cor,10,10) ## bloc correlation between active variables
Sigma <- bdiag(Soo,Sww,Soo,Sww,Soo)
diag(Sigma) <- 1
n <- 50
x <- as.matrix(matrix(rnorm(95*n),n,95) \%*\% chol(Sigma))
y <- 10 + x \%*\% beta + rnorm(n,0,10)

## Plot the Lasso path
plot(lasso(x,y), title="Lasso solution path")
## Plot the Elastic-net path
plot(elastic_net(x,y), title = "Elastic-net solution path")
## Plot the Elastic-net path (fraction on X-axis, unstandardized coefficient)
plot(elastic_net(x,y, lambda2=10), standardize=FALSE, xvar="fraction")
## Plot the Bounded regression path (fraction on X-axis)
plot(bounded_reg(x,y, lambda2=10), xvar="fraction")
}
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-QuadrupenFit-clone"></a>}}
\if{latex}{\out{\hypertarget{method-QuadrupenFit-clone}{}}}
\subsection{\code{QuadrupenFit$clone()}}{
  The objects of this class are cloneable with this method.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{QuadrupenFit$clone(deep = FALSE)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{deep}}{Whether to make a deep clone.}
    }
    \if{html}{\out{</div>}}
  }
}

}
